export interface slidePropsType {
  id: string;
  image: number;
  title: string;
  subTitle?: string;
}

export interface HistoryItem {
  id: string;
  location: string;
  date: string;
  notes: string;
}
